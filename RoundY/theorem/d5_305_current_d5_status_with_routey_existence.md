# D5 305 Current Status With RouteY-Existence

This note is the short whole-branch status summary after the promotion of the
parallel `RouteY-Existence` layer.

## 1. What remains canonical

The canonical current D5 proof frontier is still the `RoundY` packet:

- `284/285/286` for the reader-facing manuscript and local promoted-collar
  layer
- `299/300/301/302/303` for the current resonant pure color-`1` status,
  especially the late exact promotions and the master `H_m` hinge theorem

That remains the main answer to:

- what is currently theorem-level closed in the main D5 proof program;
- what remains the live resonant residual burden;
- what another researcher should read first.

## 2. What RouteY-Existence adds

`RouteY-Existence` is a parallel existence/seam-surgery layer extracted from
the March 28 consolidated support archive.

It adds a different but useful reading of the resonant residual problem:

- treat local visibility as baseline input;
- isolate the generic-late good branch after the `7`-split;
- factor the reduced base machine into
  `active first-special-hit + constant splice`;
- expose the explicit defect three-block law in the `15r+3 / 15r+9` classes;
- and interpret the remaining global arithmetic as a finite seam-surgery
  problem rather than as a universal global odometer.

## 3. What is genuinely theorem-order in that parallel lane

Inside `RouteY-Existence`, the strongest genuinely proof-carrying arithmetic
core is:

1. symbolic generic-late base-splice `7`-split;
2. odometer reduction for the good branch reduced base machine;
3. explicit defect three-block theorem in the `15r+3 / 15r+9` classes;
4. explicit reduced base-machine theorem in those classes;
5. split-branch componentwise odometer conjugacy for `N_t`;
6. global-odometer no-go for the full reduced map `g_t`.

These points are enough to support the current slogan:

> hidden `+5` clock + finite seam surgery

for the generic-late good branch.

## 4. What is still not proved there

`RouteY-Existence` does not close the current main D5 theorem.

Its honest open core is still:

- the final global cycle theorem for the explicit seam-glued reduced base
  machine;
- the `5 | m` branch;
- and integration of that arithmetic lane back into the canonical resonant
  residual theorem.

So `RouteY-Existence` should be read as a stabilized support program, not as a
replacement proof of the current live RoundY frontier.

## 5. Best current whole-branch reading

The cleanest global split is now:

- `RoundY`:
  canonical main proof frontier;
- `RouteY-Existence`:
  parallel existence / seam-surgery arithmetic lane;
- raw `tmp/*.tar` archives:
  exhaustive historical support and exploratory provenance.
