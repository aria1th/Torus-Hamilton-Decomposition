# D5 078 Researcher A Request

## Role

You are working on **global component structure**.

This is now the main structural frontier in odd-`m`, `d=5`.

## Accepted context

Treat the following as accepted:

- the theorem package through the phase-corner machine;
- the safe global bridge `(beta,rho)`;
- the componentwise concrete bridge `(beta,q,sigma)` / `(beta,delta)`;
- the `077` Track B reduction: fixed-`delta` ambiguity is now a tail-length /
  component-geometry question, not a local event-law question.

## Your exact question

What are the true splice-connected accessible regular components?

In particular:

- are the source-family windows genuine components, or overlapping charts on a
  larger total component?
- what is the actual exit-to-entry splice map through the patched region?
- what really happens at the exceptional sector / patched exit?

## Strongest useful outcome

Best case:

- prove the actual splice-connected component structure, or
- prove there is one relevant total component carrying raw global `(beta,delta)`.

Still-useful case:

- identify the exact unresolved splice point and give a theorem-style reduction
  of the component question to one explicit patched exit map.

## Things not to spend time on

- rediscovering local bridge readout formulas;
- redoing abstract realization theory;
- broad local-controller or witness search.

## Suggested starting files

1. `RoundY/theorem/d5_078_accepted_frontier_and_split.md`
2. `RoundY/theorem/d5_077_globalization_handoff.md`
3. `tmp/077_d5_trackA_analysis_note.md`
4. `tmp/d5_076_bridge_main.md`
5. `tmp/d5_076_concrete_bridge_proof.md`
6. `RoundY/checks/d5_077_compact_interval_summary.json`
7. `artifacts/d5_exact_reduction_support_068b/data/marked_chain_validation.json`
8. `artifacts/d5_exact_reduction_support_068b/data/branch_support_extension.json`
9. `artifacts/d5_phase_scheduler_branch_support_059b/data/large_range_branch_validation.json`

## Deliverable

Please return:

- `d5_078_rA.md`

Optional:

- `.tar.gz` if you package diagrams, splice tables, or structured exports.
