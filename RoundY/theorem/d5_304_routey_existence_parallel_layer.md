# D5 304 RouteY-Existence Parallel Layer

This note records the promotion of the March 28 consolidated support archive
into a stable parallel layer:

- [../../RouteY-Existence/README.md](../../RouteY-Existence/README.md)

## What Was Promoted

The new `RouteY-Existence` directory extracts the proof-carrying existence and
generic-late arithmetic spine from
`tmp/d5_consolidated_support_archive_2026-03-28_v3.tar`.

The promoted layer keeps:

- the archive status docs
- the resonant manuscript snapshot
- the existence-oriented notes
- the generic-late arithmetic chain
- the shortest matching exact reports and replay scripts

It does not replace the raw tar. The raw archive still contains older bundles,
exploratory superseded material, and larger duplicate support files.

## How To Read It

The intended order is:

1. `status/MAIN_FLOW.md`
2. `status/CURRENT_STATUS.md`
3. `status/KNOWN_CORRECTIONS_AND_SUPERSESSIONS.md`
4. `manuscript/d5_resonant_current_manuscript_2026-03-27.tex`
5. the existence notes
6. the generic-late arithmetic chain

## What This Changes

This promotion does **not** change the canonical current D5 frontier in
`RoundY`.

Instead it makes a separate reading explicit:

- local visibility is treated as closed baseline input;
- the generic-late good branch is read as an explicit reduced base-machine
  problem;
- the right abstraction is hidden `+5` clock plus finite seam surgery, not one
  universal global odometer;
- the open core in that lane is a global cycle theorem for the explicit
  seam-glued reduced machine.

So `RouteY-Existence` is a parallel existence/seam-surgery layer, while the
main current proof frontier remains the `284/285/286` and `299–303` RoundY
packet.
