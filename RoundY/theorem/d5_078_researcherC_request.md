# D5 078 Researcher C Request

## Role

You are working on **actual-union compute support**.

This is no longer broad search. The goal is to decide the globalization
question on actual regenerated unions, not on proxy models.

## Accepted context

Treat the following as accepted:

- safe global bridge: abstract `(beta,rho)`;
- strongest componentwise concrete model: `(beta,delta)`;
- local bridge readout is no longer the main uncertainty;
- the only remaining compute question is whether repeated realized `delta`
  values on the true accessible union carry one future word or several.

## Your exact question

What do the **actual regenerated regular unions** do on
`m = 13,15,17,19,21`?

Target objects, in order of usefulness:

1. chain-level splice graph between regular carry-jump chains;
2. explicit component IDs / component decomposition;
3. row-level successor table with enough fields to reconstruct chain IDs;
4. compact per-source export:
   realized `delta` interval, endpoint successor target, overlap/component info.

## Strongest useful outcome

Best case:

- direct actual-union evidence that repeated realized `delta` values have the
  same future-word content and one consistent continuation.

Negative case:

- first actual-union witness that the same realized `delta` occurs with two
  different tail lengths or two different valid continuations.

## Things not to spend time on

- broad controller search;
- reopening old tiny catalogs;
- speculative new bridge parametrizations unless they directly test the
  globalization criterion.

## Suggested starting files

1. `RoundY/theorem/d5_078_accepted_frontier_and_split.md`
2. `RoundY/theorem/d5_077_live_questions_and_tracks.md`
3. `tmp/d5_077_trackC_actual_union_check_20260314.md`
4. `tmp/077_d5_trackC_work_20260314.md`
5. `RoundY/checks/d5_077_compact_interval_summary.json`
6. `artifacts/d5_exact_reduction_support_068b/data/marked_chain_validation.json`
7. `artifacts/d5_exact_reduction_support_068b/data/accessible_quotient_on_chain.json`
8. `artifacts/d5_exact_reduction_support_068b/data/branch_support_extension.json`
9. `artifacts/d5_phase_scheduler_branch_support_059b/data/large_range_branch_validation.json`
10. `artifacts/d5_phase_scheduler_branch_support_059b/data/representative_branch_tables.json`
11. `scripts/torus_nd_d5_exact_reduction_support_068.py`

## Deliverable

Please return:

- `d5_078_rC.md`

Optional but encouraged:

- `d5_078_rC_artifacts.tar.gz`

The most useful artifact is a compact reusable export, not a large raw dump.
