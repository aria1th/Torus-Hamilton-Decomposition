# D5 082 Exceptional Row Reduction

This note promotes the stable results of the `081b/082` round from `tmp/` into
the RoundY theorem record.

Its purpose is to state cleanly:

1. the strongest conditional theorem now available,
2. the strongest honest unconditional reduction,
3. the exact remaining bottleneck.

## 1. Conditional globalization theorem

### Theorem 1.1

Assume every actual lift of the exceptional cutoff row `3m-3` glues through

`3m-2 -> 3m-1`

into the regular continuing class.

Then:

- no mixed-status `delta` exists on the true accessible union;
- fixed realized `delta` determines tail length / endpoint class;
- `rho = rho(delta)` globally;
- raw global `(beta,delta)` is exact.

### Reason

By the promoted `081` result, all regular cutoff rows are already closed.
So once the exceptional row is closed, the end-gluing criterion gives the full
global bridge upgrade immediately.

## 2. Reduction to a single exceptional row

### Theorem 2.1

The full raw globalization theorem is equivalent to one exceptional-row
statement:

> every actual lift of the exceptional cutoff `3m-3` must glue into the
> regular continuing class through `3m-2 -> 3m-1`.

Equivalently:

- there is no hidden second endpoint sheet over `delta = 3m-1`;
- the exceptional row cannot launch a new tail class.

### Meaning

After `081b/082`, the frontier is no longer:

- bridge discovery,
- regular-union ambiguity,
- chart-level exceptional landing,
- or a many-row end-gluing table.

It is one explicit exceptional row.

## 3. Strongest honest unconditional statement

The strongest honest current unconditional statement is:

- all regular cutoff rows are already closed;
- the full raw globalization theorem is equivalent to one exceptional
  actual-lift gluing lemma.

This is the smallest honest remaining theorem target.

## 4. Practical theorem language

Until the exceptional row is proved at raw-state level, the safe final theorem
language remains:

- global theorem: abstract `(beta,rho)`;
- componentwise theorem: concrete `(beta,delta)`;
- upgrade clause: if the exceptional actual-lift gluing lemma holds, then raw
  global `(beta,delta)` is exact.

## 5. Promoted references

This note promotes the substance of:

- `tmp/d5_081b_082_rA_actual_lift_end_gluing_theorem.md`
- `tmp/d5_081b_082_actual_lift_end_gluing_reduction.md`
- `tmp/d5_081b_082_b.md`
- `tmp/d5_081b_082_c.md`
