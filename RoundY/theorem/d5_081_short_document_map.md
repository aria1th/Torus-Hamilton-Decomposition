# D5 081 Short Document Map

This is a very short map of the `081` round.

## What each document explains

### `4.1`

The regular-union lemma.

Main point:
once the accepted handoff package is granted, every **regular** realization of
every realized `delta` continues.

Meaning:
the regular union contributes no mixed-status / tail-length ambiguity.

### `4.2`

The exceptional-interface reduction.

Main point:
the remaining exceptional gluing problem is equivalent to ruling out a hidden
second endpoint sheet launched at the regular source-`1` start label
`delta = 3m-1`.

Meaning:
the exceptional problem is no longer broad; it is one tiny interface issue.

### `5.0`

The correct proof object.

Main point:
the right finite proof object is an **end-gluing table**, but it must be built
on the **actual lifts** in the true accessible union, not only on the
reconstructed quotient.

Meaning:
the remaining proof must certify gluing row by row at the raw-state level.

### `6.0`

The targeted compute summary.

Main point:
on the reconstructed larger chart model, the only mixed labels are exactly the
cutoff labels, and every cutoff label already has many same-`delta`
continuing witnesses.

Meaning:
the finite list of rows that still need raw-state gluing is now explicit.

## Final problem

The final remaining problem is:

> prove that every actual lift of each cutoff row glues into the continuing
> class on the true accessible union.

Equivalently:

- no hidden second endpoint sheet at fixed realized `delta`;
- no mixed-status `delta` on the true accessible union;
- `rho = rho(delta)` globally;
- raw global `(beta,delta)` is exact.

In the smallest form, the live bottleneck is now:

> the actual-lift end-gluing theorem, especially at the exceptional interface
> `3m-2 -> 3m-1`.
