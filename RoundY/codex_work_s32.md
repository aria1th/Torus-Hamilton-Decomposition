Task ID:
D5-SIGNATURE-DIAG-AND-COUPLED-MODESWITCH-014

Question:
Stage A:
Does the exact predecessor-tail local signature on layer 2 actually change any low-layer return data beyond the current named three-flag partition in the fixed-twist / pure-alt-4 setting?

Stage B:
If a new exact-signature bit is extracted, can that bit be used as a **coupled mode-switch controller** for layer 3 — rather than as a finer layer-2 coloring — to produce a clean strict mixed witness with improved cycle compression on `m=5,7,9`?

Purpose:
`013` strongly suggests that fixed twist + pure alt-4 + layer-2-only refinement may already be trapped by a smaller dynamical quotient. Before launching a large exhaustive exact-signature search on layer 2, first test sterility directly. Then pivot the main computation to the more plausible live branch: a coupled geometry–twist mode-switch in which the same local bit can affect twist over a nontrivial orbit.

Inputs / Search space:
- Fixed anchors:
  - layer `0` anchor `1`
  - layer `1` anchor `4`
  - layer `4+` anchor `0`

- Fixed canonical mixed twist gadget:
  - layer-3 predecessor flag: `pred_sig1_wu2`
  - layer-3 old bit: `q+u=-1`
  - canonical slice pair:
    - `p3=0 -> 0/3`
    - `p3=1 -> 3/0`
  - control sign check on best survivors only:
    - `p3=0 -> 3/0`
    - `p3=1 -> 0/3`

- Fixed strong cycle-capable layer-2 base:
  - primary base: `w+u=2 : 2/4`
  - control base: `w+u=2 : 4/2`

- Fixed pure alt-4 layer-2 mode pool:
  \[
  A_4=
  \{
  q=-1:2/4,\ q=-1:4/2,\ w+u=2:2/4,\ w+u=2:4/2
  \}
  \]

- Named layer-2 partitions already known to be insufficient:
  - `pred_sig1_wu2`
  - `pred_sig4_wu2`
  - `pred_any_phase_align`
  - `pred_sig0_phase_align`
  - their tested two-flag and three-flag combinations from `012/013`

- Exact layer-2 object to extract:
  - predecessor-tail local signature
    \[
    \sigma_2
    \]

- Stage A observables:
  for each class of `\sigma_2`, and especially inside each coarse named-partition cell, record:
  - 3-step low-layer word
  - induced return increment
  - pilot `U_0` cycle signature
  - pilot monodromy signature

- Stage B coupled controller:
  - choose one bit `p` derived from `\sigma_2`
  - priority:
    1. coarsest exact bit not generated by the named flags
    2. if Stage A finds a dynamically distinguishing splitter, use that one first

- Stage B layer-3 mode-switch family:
  - layer-3 old bit:
    - start with `q+u=-1`
    - widen only if needed to
      \[
      \{q+u=1,\ q+u=-1,\ u=-1\}
      \]
  - layer-3 validated slice palette:
    \[
    \{0/3,\ 3/0,\ 3/3\}
    \]
  - let the layer-3 rule depend on `(s_3,p)`
  - first search the full table on the 4 contexts `(s_3,p)\in\{0,1\}^2`
  - optionally require at least two distinct slice modes to avoid trivial collapse

- Optional tiny synchronized layer-2 coupling:
  - only if pure layer-3 coupling is too weak
  - allow one designated local class to flip the layer-2 base orientation
    `2/4 \leftrightarrow 4/2`
  - do not introduce a general layer-2 table search at this stage

- Pilot moduli:
  - `m in {5,7,9}`

- Optional stability spot-checks:
  - `m in {11,13}` for the best survivors

Allowed methods:
- exact extraction of the layer-2 predecessor-tail local signature `\sigma_2`
- exact comparison of `\sigma_2` with the named-flag partitions
- Stage A sterility test by comparing return observables inside coarse named cells
- if Stage A is negative, formalize the sterility statement and prune the old layer-2-only exact-signature exhaustive branch
- Stage B:
  - exact exhaustive enumeration over the small coupled mode-switch family
  - direct full-color Latin testing
  - clean-frame filtering
  - strict-clock validation
  - exact `U_0` cycle and monodromy analysis
- comparison against existing exact baselines:
  - mixed baseline `21`
  - anti-compressive classes `35,39,57,119,137`
  - monodromy-only `155`
  - earlier cycle-only compression baseline `15`
- control-twist verification only on the best surviving rules

Success criteria:
1. Stage A:
   determine whether `\sigma_2` changes any of
   - 3-step low-layer word,
   - induced return increment,
   - pilot cycle signature,
   - pilot monodromy signature
   beyond the current named three-flag partition.

2. If Stage A shows no such change:
   state the sharpest supported pruning lemma of the form
   “fixed-twist layer-2-only exact signature is dynamically sterile.”

3. Stage B:
   find a clean strict mixed survivor on `m=5,7,9` with
   - nonzero monodromy on every pilot modulus, and
   - improved cycle structure:
     \[
     \text{total pilot cycle count} < 21
     \]
     or fewer than `m` cycles on at least one pilot modulus.

4. Stronger success:
   move materially toward the earlier cycle-only compression baseline `15` while preserving nonzero monodromy.

5. Save:
   - the exact `\sigma_2` definition / quotient used,
   - the sterility diagnostic tables,
   - the explicit coupled rule table,
   - and the full validation summary.

Failure criteria:
- Stage A shows that `\sigma_2` contributes no new low-layer return data beyond the current named partition, and
- Stage B still produces only the old regime set or no improved mixed witness, and
- no tiny synchronized layer-2 flip repairs this.

Artifacts to save:
- code
- raw logs
- summary report
- discovered examples / counterexamples
- exact layer-2 local-signature class tables
- sterility diagnostic tables
- survivor-count tables for the coupled mode-switch family
- validation outputs
- proof-supporting computations

Return format:
- exact canonical twist gadget used
- exact definition of `\sigma_2`
- comparison between `\sigma_2` and the named-flag partitions
- sterility verdict for the old layer-2-only exact-signature branch
- chosen signature-derived bit `p`
- exact coupled mode-switch family searched
- survivor counts by coupled rule
- best mixed witness found
- comparison with:
  - the `008/009/010/011/012/013` mixed baselines
  - the earlier strongest cycle-only witness
- strongest supported conclusion
- recommended next widening only if the coupled family still fails

Reproducibility requirements:
- fixed pilot moduli `5,7,9`
- deterministic enumeration order
- saved JSON summaries for:
  - exact signature classes
  - sterility diagnostic tables
  - all coupled rules tested
- exact validation script for every reported survivor
- explicit comparison against the current mixed baseline and the earlier cycle-only baseline
