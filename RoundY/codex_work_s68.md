# Codex Work Note s68

## Task

Run the optional `057A` compute check for the auxiliary `delta`-based
flat-corner lemma and the resulting `CJ` reduction.

## Result

The `delta` flat-corner support extends cleanly to
`m = 13,15,17,19,21,23`.

- the one-step flat-corner event law stays exact
- the derived flat countdown formula stays exact
- the `CJ` noncarry reduction through the flat successor with `delta = m-2`
  stays exact

## Interpretation

This is strong proof support for `057`. It does not prove the uniform theorem
yet, but it makes the `CJ` branch look genuinely reducible to one auxiliary
flat-corner lemma.
