# d=5 current frontier and start-here summary after explicit portal-packet coverage of the full stable common `m=15r+9` sector (2026-04-05)

## 1. The theorem floor is unchanged

The April 1 handoff remains the baseline:
grouped-carrier / lift / weighted baseline reduction / `\beta_m` reconstruction / explicit first-rung formula.

So the active common problem is still a problem **above** the theorem floor.

## 2. What is now explicit on the common branch

For the stable residual core of class
\[
m=15r+9,
\]
every stable residue sector now has an explicit portal-level packet:

- denominator `8`:
  - `r=8s` exact ordered `120`-portal transducer,
  - `r=8s+4` exact ordered `120`-portal transducer;
- denominator `16`:
  - `r\equiv 6,14 \pmod{16}` explicit stable `240`-portal packets with handwritten exit theorems and explicit interior packets,
  - `r\equiv 2,10 \pmod{16}` shared-lexicon explicit stable `240`-portal packets with handwritten terminal exits and explicit interior tables.

So the common branch is no longer waiting on residue-family discovery.

## 3. What remains live

The honest live burden is now promotion / compression:

1. handwritten local theorems for the still-checked portal sectors (especially denominator `8`);
2. shorter seam/gluing theorems compressing the explicit finite portal packets;
3. global quotient promotion as the projection of those explicit local portal machines.

## 4. Boundary

This does **not** mean the common branch is closed.
It means the local residue-wise geometry is now explicit enough that the remaining work is chiefly conceptual compression and handwritten promotion.
